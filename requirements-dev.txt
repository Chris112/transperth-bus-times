# Dependencies for running the API contract tests.
# The service itself runs inside PyScript (which supplies its own requests).
requests>=2.28.0
pytest>=7.0.0
pytest-xdist>=3.0.0  # parallel test execution
